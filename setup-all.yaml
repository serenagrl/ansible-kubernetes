# Author: Serena Yeoh
#
# Disclaimer:
# This playbook was written based on my self-learning and may not follow certain
# best practices or work properly in your environment. Use it at your own risk.
#

  # Setup virtual environment in Hyper-V by installing from scratch
- ansible.builtin.import_playbook: setup-servers.yaml

  # Setup NFS server
- ansible.builtin.import_playbook: setup-nfs.yaml

  # Setup kubernetes cluster 
- ansible.builtin.import_playbook: setup-kubernetes.yaml

  # Setup kubernetes add-on components
- ansible.builtin.import_playbook: setup-components.yaml
