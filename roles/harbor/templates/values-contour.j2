fullnameOverride: harbor
logLevel: warning
expose:
  type: clusterIP
  tls:
    enabled: true
    auto:
      commonName: {{ domain }}
    certSource: secret
    secret:
      secretName: "httpproxy-tls"
      notarySecretName: "httpproxy-tls"

externalURL: https://core.{{ domain }}

persistence:
  enabled: true

caSecretName: "httpproxy-tls"