NAMED_CONF_LOCAL_TEMPLATE: "{{ lookup('template', (ansible_distribution | lower) + '/named.conf.local.j2') }}"
FORWARD_LOOKUP_TEMPLATE: "{{ lookup('template', (ansible_distribution | lower) + '/forward.j2') }}"
REVERSE_LOOKUP_TEMPLATE: "{{ lookup('template', (ansible_distribution | lower) + '/reverse.j2') }}"
