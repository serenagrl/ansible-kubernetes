CREDENTIALS_TEMPLATE: "{{ lookup('template', 'credential-' + storage_provider + '.j2') }}"
STORAGE_LOCATION_TEMPLATE: "{{ lookup('template', 'storage-location-' + storage_provider + '.j2') | from_yaml }}"