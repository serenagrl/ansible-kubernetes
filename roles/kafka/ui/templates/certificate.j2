apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kafka-ingress-cert
  namespace: {{ kafka.namespace }}
spec:
  dnsNames:
    - kafka.{{ kube.cluster.domain }}
  issuerRef:
    kind: ClusterIssuer
    name: {{ fact_cluster_issuer_name }}
  secretName: kafka-ingress-tls