apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: kafka-httpproxy
  namespace: {{ kafka.ui.namespace }}
spec:
  virtualhost:
    fqdn: {{ kafka.ui.domain }}
    tls:
      secretName: kafka-ingress-tls
  routes:
    - conditions:
        - prefix: /
      services:
        - name: kafka-kafka-ui
          port: 80