# Launch operator.
- ansible.builtin.include_role:
    name: kafka/strimzi

# Install cluster.
- ansible.builtin.include_role:
    name: kafka/cluster

# Install bridge.
- ansible.builtin.include_role:
    name: kafka/bridge