apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-minio-ingress-cert
  namespace: gitlab
spec:
  dnsNames:
    - gitlab.{{ domain }}
    - gitlab-console.{{ domain }}
  issuerRef:
    kind: ClusterIssuer
    name: default-cluster-issuer
  secretName: gitlab-minio-ingress-tls