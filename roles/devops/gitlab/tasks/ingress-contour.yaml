- name: Create httpproxies for Gitlab services
  kubernetes.core.k8s:
    apply: yes
    force: yes
    definition: "{{ item }}"
  loop: "{{ HTTP_PROXY_TEMPLATES }}"
  when: 'not "minio" in item.metadata.name or ("minio" in item.metadata.name and not gitlab.external_minio)'