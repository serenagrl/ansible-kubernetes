apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gitlab-ingress-cert
  namespace: gitlab
spec:
  dnsNames:
    - gitlab.{{ _kube.cluster.domain }}
    - kas.{{ _kube.cluster.domain }}
    - minio.{{ _kube.cluster.domain }}
    - registry.{{ _kube.cluster.domain }}
  issuerRef:
    kind: ClusterIssuer
    name: {{ fact_cluster_issuer_name }}
  secretName: gitlab-ingress-tls