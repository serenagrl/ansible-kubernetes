apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mssql-cert
  namespace: {{ mssql.target_namespace }}
spec:
  dnsNames:
    - {{ mssql.domain_prefix }}.{{ _kube.cluster.domain }}
  issuerRef:
    kind: ClusterIssuer
    name: {{ fact_cluster_issuer_name }}
  secretName: mssql-tls