- name: Setup ingress
  ansible.builtin.include_tasks: ./tasks/task-create-ingress-contour.yaml
  vars:
    name: kubernetes-dashboard
    namespace: kubernetes-dashboard
    definition: "{{ HTTP_PROXY_TEMPLATE }}"