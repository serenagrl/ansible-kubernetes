NGINX_KIBANA_VALUES_FILE: "{{ lookup('template', 'kibana-values-nginx.j2') | from_yaml }}"
CONTOUR_KIBANA_VALUES_FILE: "{{ lookup('template', 'kibana-values-contour.j2') | from_yaml }}"
ELASTICSEARCH_VALUES_FILE: "{{ lookup('template', 'elasticsearch-values.j2') | from_yaml }}"
ELASTICSEARCH_CERTIFICATE_TEMPLATE: "{{ lookup('template', 'elasticsearch-certificate.j2') | from_yaml }}"
MANIFEST_FILE: "{{ _work_dir }}/fluentd.yaml"
INGRESS_CONTOUR_TEMPLATE: "{{ lookup('template', 'ingress-contour.j2') | from_yaml }}"

INSTALL:
  contour: "kibana-install-contour.yaml"
  nginx: "kibana-install-nginx.yaml"