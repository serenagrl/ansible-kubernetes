MANIFEST_FILE: "{{ _work_dir }}/rmq-cluster-operator.yaml"
CERTIFICATE_TEMPLATE: "{{ lookup('template', 'certificate.j2') | from_yaml }}"
CLUSTER_TEMPLATE: "{{ lookup('template', 'rabbitmq-cluster.j2') | from_yaml }}"