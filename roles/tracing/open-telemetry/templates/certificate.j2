apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opentelemetry-ingress-cert
  namespace: tracing
spec:
  dnsNames:
    - otel.{{ kube.cluster.domain }}
  issuerRef:
    kind: ClusterIssuer
    name: {{ fact_cluster_issuer_name }}
  secretName: opentelemetry-ingress-tls