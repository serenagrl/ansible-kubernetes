# Launch operator.
- ansible.builtin.include_role:
    name: rabbitmq/operator

# Install cluster.
- ansible.builtin.include_role:
    name: rabbitmq/cluster

# Install topology.
- ansible.builtin.include_role:
    name: rabbitmq/topology