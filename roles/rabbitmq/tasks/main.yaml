# Launch operator.
- ansible.builtin.include_role:
    name: rabbitmq/operator

# Install topology.
- ansible.builtin.include_role:
    name: rabbitmq/topology

# Install cluster.
- ansible.builtin.include_role:
    name: rabbitmq/cluster