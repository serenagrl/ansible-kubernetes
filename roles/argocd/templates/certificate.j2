apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ certificate_name }}
  namespace: argocd
spec:
  dnsNames:
    - {{ domain }}
  issuerRef:
    kind: ClusterIssuer
    name: default-cluster-issuer
  secretName: {{ secret_name }}