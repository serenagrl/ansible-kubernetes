apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: root-httpproxy
  namespace: projectcontour
spec:
  virtualhost:
    fqdn: {{ ingress_host }}
    tls:
      secretName: root-httpproxy-tls
  includes: []