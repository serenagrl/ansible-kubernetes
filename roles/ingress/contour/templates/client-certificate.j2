apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: contour-client-cert
  namespace: projectcontour
spec:
  dnsNames:
    - client-certificate
  issuerRef:
    kind: ClusterIssuer
    name: {{ fact_cluster_issuer_name }}
  secretName: contour-client-tls