remote_admin:
  # Specify whether to install the latest kubectl on the Ansible controller host.
  install_kubectl: yes

  # Specify whether to install k9s on the Ansible controller host.
  install_k9s: yes
  
  # Specify whether to install krew on the Ansible controller host.
  install_krew: yes