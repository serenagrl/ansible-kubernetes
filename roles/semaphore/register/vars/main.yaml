semaphore:
  project:
    name: "Ansible-Kubernetes"
    local_repo_dir: /ansible-playbooks/ansible-kubernetes

    create:
      misc_environments: yes
      misc_playbooks: yes

  api_login:
      username: admin
      password: p@ssw0rd
