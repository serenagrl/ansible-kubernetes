# Trivy environment variables.
VAULT_VARS:
  - { name: vault, vars: "{{ var_files | community.general.json_query('results[?contains(source, `/roles/vault/vars`)].content') | b64decode | from_yaml }}" }