MANIFEST_FILE: "{{ temp_dir }}/cert-manager.yaml"
CERT_DIR: "{{ temp_dir }}/{{ kube.cluster.name }}"
KEY_FILE: "{{ CERT_DIR }}/{{ kube.cluster.name }}-ca.key"
CRT_FILE: "{{ CERT_DIR }}/{{ kube.cluster.name }}-ca.crt"
