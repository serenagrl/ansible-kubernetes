NAMES:
  CP: "{{ groups['kubernetes_control_planes'].0 }}"
  INVENTORY: "{{ awx.setup.name }} Inventory"
  PROJECT: "{{ awx.setup.name }} Project"
  JOB_TEMPLATE: "{{ awx.setup.name }} Job Template"
  HOST_CREDENTIAL: "{{ awx.setup.name }} {{ groups['kubernetes_control_planes'].0 }} Host Credential"
  SCM_CREDENTIAL: "{{ awx.setup.name }} Source Control Credential"
  API_CREDENTIAL: "{{ awx.setup.name }} Gitlab API Access Token"
AWX_HOST: "https://{{ _kube.hostname }}/awx/"