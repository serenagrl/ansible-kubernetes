kubernetes_control_plane:
  hosts: 
    k8s-cp:
      ansible_host: 192.168.0.200
      ansible_user: root

    # Uncomment this if you need redundant control planes. Otherwise leave it to conserve resources.
    # k8s-cp2:
    #   ansible_host: 192.168.0.220
    #   ansible_user: root      